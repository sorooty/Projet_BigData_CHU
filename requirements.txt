# Packages supplémentaires au-delà de ce que l'image Airflow 2.9.2 fournit.
# NE PAS ajouter: apache-airflow, apache-airflow-providers-*, SQLAlchemy
# (déjà inclus dans l'image — les réinstaller crée des conflits de version).

# Data processing
pandas==2.2.2
pyarrow==16.1.0

# Database (driver PostgreSQL pour les scripts extract/load)
psycopg2-binary==2.9.9

# Utilities
python-dotenv==1.0.1
